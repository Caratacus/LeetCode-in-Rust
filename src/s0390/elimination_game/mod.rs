// Problem 0390: elimination game

pub struct Solution;

impl Solution {
    pub fn last_remaining(n: i32) -> i32 {
        todo!()
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    // Java: void lastRemaining()
    //   assertThat(new Solution().lastRemaining(9), equalTo(6));
    #[test]
    fn test_last_remaining() {
        // TODO: 翻译 Java 测试
    }

    // Java: void lastRemaining2()
    //   assertThat(new Solution().lastRemaining(1), equalTo(1));
    #[test]
    fn test_last_remaining2() {
        // TODO: 翻译 Java 测试
    }
}
