// Problem 0933: number of recent calls

pub struct RecentCounter {}

impl RecentCounter {
    pub fn new() -> Self {
        todo!()
    }

    pub fn ping(&mut self, t: i32) -> i32 {
        todo!()
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    // Java: void recentCounterTest()
    //   RecentCounter recentCounter = new RecentCounter();
    //   assertThat(recentCounter.ping(1), equalTo(1));
    //   assertThat(recentCounter.ping(100), equalTo(2));
    //   assertThat(recentCounter.ping(3001), equalTo(3));
    //   assertThat(recentCounter.ping(3002), equalTo(3));
    #[test]
    fn test_recent_counter_test() {
        // TODO: 翻译 Java 测试
    }
}
